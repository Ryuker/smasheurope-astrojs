---
import { Icon } from 'astro-icon/components';
const {
  icon, 
  text, 
  url,
  classes = " ",
  colors = 'bg-se-blue-100 hover:bg-se-blue-200', 
  newTab = false
} = Astro.props;

---

<a target={newTab} href={url} class={`${classes} ${colors} px-6 py-3 rounded-lg flex justify-center`}>
  {icon && <Icon name={icon} class="text-lg" />}
  {text && text}
</a>