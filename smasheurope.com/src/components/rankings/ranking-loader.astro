---
import Button from '../universal/button.astro';
import { getEntry } from 'astro:content';

const ultRanking = await getEntry('rankings', 'ultimate/01_ult-rank-2023');

const  {
  ranking } = Astro.props;

---
<section id="rankings" class="container">
  
  <h1>Rankings</h1>
  <h2>{ranking.data.game} - Ranking</h2>
  <ul>
    { ranking.data.rankedPlayers.slice(0,5).map((player:any, index:any) => (
        <li>
          {player.rank} | 
          {player.tag} - 
          {player.name} - 
          {player.country} - 
          chars: 
          {player.characters.map((character:String, index:any) => (
            index == 0 ? character + " , " : character
            ))
          }
        </li>
      ))
    }
  </ul>
  <div>
    <Button name="full ranking" icon="fas fa-award" url="/rankings/ultimate-ranking"/>
  </div>
</section>

<style>
  .container {
    display: flex;
    justify-content: center;
    flex-direction: column;
    max-width: inherit;
    margin-bottom: 50px;
  }

  ul {
      list-style: none;
  }
</style>
